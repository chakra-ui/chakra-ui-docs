export const TutorialApp1 = `import { Center, ChakraProvider, Heading, theme } from '@chakra-ui/react'

export default function App() {
  return (
    <ChakraProvider theme={theme}>
      <div>Hello World!</div>
    </ChakraProvider>
  )
}`
